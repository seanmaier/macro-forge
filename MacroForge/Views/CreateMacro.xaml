<Window x:Class="MacroForge.Views.CreateMacro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:MacroForge"
        xmlns:views="clr-namespace:MacroForge.Views"
        mc:Ignorable="d"
        Title="CreateMacro" Height="700" Width="600"
        ResizeMode="NoResize" WindowStyle="None"
        Background="Transparent" AllowsTransparency="True">
    <Border Background="#eff2f7" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <views:TitleBarControl Grid.ColumnSpan="2" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Create Macro" Margin="20 5" Style="{StaticResource HeaderLabel}"/>
            
            <StackPanel Grid.Row="2" Grid.Column="0">
                <Label Content="Name" Margin="20 0"/>
                <Border Style="{StaticResource Container}">
                    <TextBox Style="{StaticResource MacroTextBox}"/>
                </Border>
            </StackPanel>
            <StackPanel Grid.Row="2" Grid.Column="1">
                <Label Content="Shortcut" Margin="20 0" />
                <Border Style="{StaticResource Container}">
                    <TextBox Style="{StaticResource MacroTextBox}"/>
                </Border>
            </StackPanel>
            <StackPanel
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Grid.Row="3">
                <Label Content="Description" Margin="20 0"/>
                <Border Style="{StaticResource Container}">
                    <TextBox  Height="180" Style="{StaticResource MacroTextBox}"/>
                </Border>
            </StackPanel>

            <StackPanel
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Grid.Row="4">
                <Label Content="Macro Steps"  Margin="20 0"/>
                <Border Style="{StaticResource Container}">
                    <ListBox Style="{StaticResource MacroListBox}" Height="180"/>
                </Border>
            </StackPanel>
            
            <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Margin="5" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Content="Cancel" Style="{StaticResource FooterButton}"/>
                <Button Content="Save" Style="{StaticResource FooterButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>